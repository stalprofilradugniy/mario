// scripts/constants.js

// --- Основные константы игры ---

// Размеры игрового мира в "пикселях" (как на оригинальной NES)
export const GAME_WIDTH = 256;
export const GAME_HEIGHT = 240;

// Размер одной плитки/клетки в игровом мире (в пикселях NES)
// Все объекты обычно привязаны к сетке плиток
export const TILE_SIZE = 16;

// Количество пикселей, на которое мир "сдвигается" за игроком
// Это влияет на то, как быстро камера следует за игроком
export const CAMERA_SPEED = 1.5; // Пример: скорость камеры

// --- Константы физики и движения ---

// Сила гравитации, влияющая на падение объектов
// Увеличивает вертикальную скорость (чем больше значение, тем быстрее падение)
export const GRAVITY = 0.5;

// Сила прыжка игрока (отрицательное значение, так как Y-координата увеличивается вниз)
export const JUMP_FORCE = -10;

// Максимальная скорость падения (чтобы объекты не ускорялись бесконечно)
export const MAX_FALL_SPEED = 10; // Пример

// Скорость горизонтального движения игрока
export const PLAYER_SPEED = 2;

// Скорость горизонтального движения врагов (например, Goomba)
export const ENEMY_SPEED = 1;

// --- Константы UI/отображения ---

// Значения очков за различные действия
export const SCORE_GOOMBA = 100;
export const SCORE_COIN = 200;
export const SCORE_FLAGPOLE_TOP = 5000; // Очки за вершину флагштока
// Добавьте очки за другие действия (разбитие блоков, другие враги и т.д.)

// Начальные игровые значения
export const START_LIVES = 3;
export const START_SCORE = 0;

// Начальная позиция игрока по умолчанию (если не указана на уровне)
// GAME_HEIGHT - TILE_SIZE * 3 разместит игрока примерно на 3 плитки выше дна экрана
export const DEFAULT_PLAYER_START_X = TILE_SIZE * 2;
export const DEFAULT_PLAYER_START_Y = GAME_HEIGHT - TILE_SIZE * 3;
