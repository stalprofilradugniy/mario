// scripts/constants.js

// Основные константы игры
export const GAME_WIDTH = 256;       // Ширина игрового мира (в пикселях оригинальной NES)
export const GAME_HEIGHT = 240;      // Высота игрового мира (в пикселях оригинальной NES)
export const TILE_SIZE = 16;         // Размер одной плитки/клетки в игровом мире

// Константы физики и движения
export const GRAVITY = 0.5;          // Сила гравитации, влияющая на падение объектов (чем больше, тем сильнее гравитация)
export const JUMP_FORCE = -10;       // Сила прыжка игрока (отрицательное значение, так как Y увеличивается вниз)
export const PLAYER_SPEED = 2;       // Скорость горизонтального движения игрока
export const ENEMY_SPEED = 1;        // Скорость горизонтального движения врагов
